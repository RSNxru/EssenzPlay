# --- Backend ---
DATABASE_URL=postgresql+asyncpg://streamdlp:streamdlp@db:5432/streamdlp
DOWNLOAD_DIR=/downloads
CORS_ORIGINS=["http://localhost:5173"]

# Opcional: cookies (formato Netscape) para contenido con edad/login.
# Exporta desde tu navegador con una extensión tipo "Get cookies.txt".
# COOKIES_FILE=/app/cookies.txt

# SponsorBlock: categorías a saltar (sponsor, selfpromo, intro, outro, interaction, music_offtopic)
# SPONSORBLOCK_CATEGORIES=["sponsor","selfpromo","interaction"]
